# SPDX-License-Identifier: GPL-2.0-only
#
# The external build entry point is Makefile.  It stages the target kernel's
# iwlwifi tree and invokes that tree's own Kbuild files.  Keeping this file
# empty prevents an old, hard-coded object list from being selected by DKMS.
